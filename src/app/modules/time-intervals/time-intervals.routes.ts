import { Routes } from '@angular/router';
import { TimeIntervalsContainerComponent } from './components';

export const timeIntervalRoutes: Routes = [
    {
        path: '',
        component: TimeIntervalsContainerComponent,
    },
];
